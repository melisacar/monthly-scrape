CREATE TABLE IF NOT EXISTS flights (
    airport_name        VARCHAR,
    fligt_type          VARCHAR,
    quantity            FLOAT,
    category            VARCHAR,
    fligt_time          CHAR(7),
    retrieval_at        DATE
);
